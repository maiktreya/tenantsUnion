[pytest]
# This file configures pytest for the project.

# Explicitly load the NiceGUI plugin to ensure its fixtures are always available.
addopts = -p nicegui.testing.plugin

testpaths = tests
python_files = test_*.py
asyncio_mode = auto