# Enhanced requirements.txt for robust UI testing
# Install with: pip install -r tests/requirements_robust.txt

# Core testing framework
pytest
pytest-asyncio

# For mocking HTTP requests
respx

# For managing Docker containers within integration tests
pytest-docker

# Required for NiceGUI's basic functionality
nicegui

# For testing NiceGUI interface for live edition of preview records
pandas

# Selenium for robust browser automation
selenium>=4.0.0

# HTTP client for server health checks
requests

# WebDriver manager to automatically handle driver downloads
webdriver-manager

# For process management
psutil

# Optional: For more advanced wait conditions
selenium-wire

# Optional: For taking better screenshots and debugging
pillow
